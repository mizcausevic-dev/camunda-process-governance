## Highlights
- ships a public Camunda/workflow operator surface for task routing, timer drift, approval packets, and execution review posture
- includes an offline analyzer, CLI, structured API payloads, and static dashboard routes
- publishes docs, verification copy, robots, sitemap, screenshots, and `docs/KINETIC_GAIN_EMBEDDED.md`

## Release gate
- `npm run verify`
- `npm run prerender`
- `npm run render:assets`
